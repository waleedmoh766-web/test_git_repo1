import 'package:flutter/material.dart';

class PaymentTest extends StatelessWidget {
  const PaymentTest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Payment Test')),
      body: const Center(child: Text('This is the Payment Test screen.')),
    );
  }
}
